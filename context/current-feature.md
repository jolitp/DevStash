# Current Feature: Item Create

## Status

<!-- Not Started|In Progress|Completed -->

In Progress

## Goals

<!-- Goals & requirements -->

- Add new items via a **modal dialog** (shadcn `Dialog`), opened from the "New Item" button in the top bar.
- **Type selector** for: snippet, prompt, command, note, link.
- Show fields conditionally based on the selected type:
  - All types: `title` (required), `description`, `tags`.
  - snippet / command: `content`, `language`.
  - prompt / note: `content`.
  - link: `url` (required).
- Add a `createItem` **server action** with **Zod validation** (following the existing `updateItem`/`deleteItem` pattern: `"use server"`, `{ success, data, error }`, session-guarded).
- Add a `createItem` **query function** in `src/lib/db/items.ts`.
- On success: **toast**, close the modal, and **refresh** the list.

## Notes

<!-- Any extra notes -->

- **Spec:** `context/features/019-item-create-spec.md`.
- **Pattern to mirror:** `updateItem` in `src/actions/items.ts` + its db layer in `src/lib/db/items.ts`, and the `updateItemSchema` in `src/lib/validations/items.ts` (add a sibling `createItemSchema`). The create action owns the new item, so `userId` comes from the session — no ownership check needed (unlike update/delete).
- **Type → typeId:** the type selector offers names (`snippet`/`prompt`/`command`/`note`/`link`); resolve to `ItemType.id` in the db layer (case-insensitive `findFirst` like `getItemsByType`). Note it's **`link`**, not `url`, per the seed. File/Image types are Pro-only and intentionally excluded from the create dialog.
- **Tags:** reuse the per-owner `connectOrCreate` (`userId_name`) approach from `updateItem`'s db layer.
- **UI home:** the "New Item" button lives in the top bar (currently display-only) — wire it to open the dialog. Reuse the existing shadcn setup; install the `dialog` component only if not already present.
- **Tests:** add unit tests for `createItemSchema` and the `createItem` action guards (mirroring `items.test.ts`).
- **Testing note:** creating items writes to the DB — prefer the Neon **Development** branch and clean up throwaway items, mindful of the seeded demo data.

## History

<!-- Keep this updated. EARLIEST TO LATEST -->

- 2026-07-01 — Initial Next.js 16 + React 19 + Tailwind v4 setup: scaffolded via `create-next-app`, removed default starter SVGs, expanded `CLAUDE.md`, added project context files, replaced default page with placeholder.

- 2026-07-01 — Dashboard UI Phase 1: added `src/lib/mock-data.ts` as the mock data source of truth; initialized ShadCN (base-nova, neutral, lucide) and installed `button` + `input`; enabled dark mode by default and set DevStash metadata in the root layout; built the `/dashboard` route with a top bar (display-only search + New Item button) and a shell splitting into Sidebar/Main placeholders. Verified with `pnpm build` and `pnpm lint`.

- 2026-07-01 — Dashboard UI Phase 2: built the collapsible sidebar under `src/components/dashboard/sidebar/` — `sidebar-context.tsx` (desktop-collapse + mobile-drawer state, Escape-to-close, body scroll lock), `sidebar-data.ts` (nav model derived from mock data: library links, type links to `/items/[slug]`, favorite + recent collections with item counts), `Sidebar.tsx` (brand header, Library/Types/Favorite Collections/Recent Collections sections with active-route highlighting, plan-usage card + user avatar footer), and `SidebarTrigger.tsx` (mobile drawer button). Restructured the dashboard layout to a full-height sidebar with top bar + main stacked to its right; moved the brand from the top bar into the sidebar; added a `no-scrollbar` utility in `globals.css`. Off-canvas drawer on mobile with backdrop. Verified with `pnpm build` and `pnpm lint`.

- 2026-07-01 — Dashboard UI Phase 3: built the main area under `src/components/dashboard/main/` — `dashboard-data.ts` (selectors over mock data: stats, pinned items, 10 recent items, top-4 recent collections with counts, `typeById` lookup, stable `referenceNow`), `StatsCards.tsx` (4 stat cards: items/collections/favorite items/favorite collections), `ItemCard.tsx` (type badge + type-colored left accent border, pin/favorite markers, per-type content preview, tags, relative time), `CollectionCard.tsx`, and `SectionHeader.tsx`. Added `src/lib/format.ts` (`formatRelativeTime`, `fileExtension`) and assembled the `/dashboard` page: stats → Pinned Items → Recent Collections → Recent Items grids. Server component reading mock data directly. Verified with `pnpm build` and `pnpm lint`.

- 2026-07-02 — Prisma 7 + Neon PostgreSQL setup: added the `prisma-client` generator (output `src/generated/prisma`, cjs) + `prisma.config.ts` (dotenv-loaded `DATABASE_URL`) + Neon serverless driver adapter in `src/lib/prisma.ts`; wrote the initial schema (User, Item, ItemType, Collection, Tag, ItemTag + NextAuth Account/Session/VerificationToken) with FK indexes and cascade deletes, and created/applied migration `20260702120514_init` to the Neon dev branch. Added `flake.nix`/`flake.lock` (provides the Prisma 7 `schema-engine` on NixOS), a `scripts/test-db.ts` connectivity check, `db:*` scripts (generate/migrate/deploy/studio/test), `.env.example`, and a NixOS/Prisma note in `CLAUDE.md`. Verified with `prisma validate`, `prisma migrate status`, `pnpm db:test`, `pnpm lint`, and `pnpm build`.

- 2026-07-02 — Seed data: added `prisma/seed.ts` (registered via `migrations.seed` in `prisma.config.ts`) that wipes the app tables and inserts a deterministic dataset — demo user `demo@devstash.io` (password hashed with bcryptjs @ 12 rounds), the 7 system item types, and 5 collections with 18 items (React Patterns snippets, AI Workflows prompts, DevOps, Terminal Commands, Design Resources) using real URLs and `connectOrCreate` tags. Added the `bcryptjs` dependency and a `db:seed` script; expanded `scripts/test-db.ts` to fetch and print the seeded user/types/collections/items. Verified with `pnpm db:seed` (idempotent re-run), `pnpm db:test`, `pnpm lint`, and `pnpm build`.

- 2026-07-02 — Dashboard Collections (real data): added `src/lib/db/collections.ts` with `getRecentCollections()` — fetches collections + their items/types via Prisma, sorts by latest item activity, and per card derives item count, distinct types (most-used first), and an accent color from the top type. Rewrote `CollectionCard.tsx` to take the new `DashboardCollection` shape: left accent border from the most-used type color plus a row of small type-icon badges (icon-name → lucide map covering the seeded `ItemType.icon` values). Made `/dashboard` an async server component awaiting `getRecentCollections()` and added `export const dynamic = "force-dynamic"` so it reads live DB data per request instead of prerendering stale data at build. Items grids and `StatsCards` still on mock (items not migrated yet, per spec). Verified with `pnpm lint`, `pnpm build` (dashboard now `ƒ Dynamic`), and a live render against the seeded DB (correct counts, accent colors, and multi-type badges e.g. DevOps → link·2/snippet·1/command·1).

- 2026-07-02 — Dashboard Items (real data): added `src/lib/db/items.ts` with `getDashboardItems()` — one newest-first Prisma fetch that derives the pinned grid (`isPinned`), the top-10 recent items, and a shared `referenceNow` (latest item time) for deterministic relative times; each item carries its type (name/icon/color) and flattened tag names. Added `src/lib/db/stats.ts` with `getDashboardStats()` (four parallel `count()` queries for items/collections + favorites). Rewrote `ItemCard.tsx` to take a `DashboardItem` + `referenceNow` prop (icon/border from the item type, DB icon-name→lucide map, `capitalize`d lowercase type names, null-guarded description/preview); made `StatsCards.tsx` take a `stats` prop; added `formatFileSize` to `format.ts` (DB `fileSize` is bytes). `/dashboard` now fetches stats/items/collections in parallel and only renders the Pinned section when non-empty. `dashboard-data.ts` is now unused (sidebar still reads `mock-data` directly). Verified with `pnpm lint`, `pnpm build` (dashboard still `ƒ Dynamic`), and a live render against the seeded DB (stats 18/5/2/1, type-colored badges + tags + relative times, Pinned section present).

- 2026-07-02 — Stats & Sidebar (real data): wired the sidebar to live DB data (stats/items main area already migrated in prior features). Added `src/lib/db/item-types.ts` with `getSidebarItemTypes()` (item types + per-type item counts, `name`-ordered) and `getSidebarCollections()` in `src/lib/db/collections.ts` (all favorites + top-5 recents by latest activity, each with item count, `isFavorite`, and an `accentColor` from its most-used type via a shared `topTypeColor` helper). Rewrote `sidebar-data.ts` to drop the mock imports — now just `NavLink` (with optional `dotColor`/nullable `color`), a `capitalize` helper, and the static `libraryLinks`. Made `Sidebar.tsx` props-driven (`typeLinks`/`favoriteCollections`/`recentCollections`), taught `NavItem` to render a colored dot when `dotColor` is set (recents) vs. an icon (types with DB icon-name→lucide map extended for `Code`/`Terminal`/`StickyNote`), and added a "View all collections" link (→ `/collections`) under the recents. Made the dashboard layout an async server component that fetches types + collections in parallel and maps them to nav links; type routes are `/items/[name]` per the spec, favorites keep the Star icon. Added `export const dynamic = "force-dynamic"` to the layout. Verified with `pnpm lint`, `pnpm build`, and a live render against the seeded DB (7 type links w/ counts, favorite React Patterns w/ star, 5 recents w/ correct type-color dots — snippet #3b82f6 / prompt #8b5cf6 / command #f97316 / link #10b981 — and the View all collections link).

- 2026-07-02 — Pro badge on sidebar types: installed the ShadCN `badge` component and added a subtle uppercase **PRO** badge to the File and Image item types in the sidebar (Pro-tier features per the spec). Added a `pro?: boolean` flag to `NavLink` (`sidebar-data.ts`); the dashboard layout flips it on via a `PRO_TYPES` set (`file`/`image`) when mapping item types to nav links; `NavItem` in `Sidebar.tsx` renders a compact `secondary` badge (`h-4`, `text-[10px]`, `tracking-wider`, muted foreground) before the item count, hidden when the desktop rail is collapsed. Verified with `pnpm lint` (clean) and `pnpm build` (dashboard still `ƒ Dynamic`).

- 2026-07-02 — Audit quick wins: low/no-risk cleanups from the code-scanner audit. (1) Consolidated the `icon-name → lucide component` map that was duplicated across `ItemCard.tsx`, `CollectionCard.tsx`, and `Sidebar.tsx` into a single `src/lib/item-type-icons.ts` (`ITEM_TYPE_ICONS`, the union of all three prior key sets); call sites index into the shared map directly rather than a resolver function, since a component returned from a function call during render trips the `react-hooks/static-components` ESLint rule (Record indexing does not). (2) Simplified `getDashboardItems` `referenceNow` to read the newest-first `rows[0]` instead of `reduce`-ing the whole list. (3) Moved the seeded demo password to `SEED_DEMO_PASSWORD` (dev-only, safe `"12345678"` fallback) and documented it in `.env.example`. Deferred M1 (unbounded collection fetches), M3 (sidebar user still on mock data — blocked on auth), L2 (splitting `Sidebar.tsx`), and L3 (`ItemGrid` extraction). Verified with `pnpm lint` (clean) and `pnpm build` (dashboard still `ƒ Dynamic`); confirmed all seed type icons + sidebar library-link icons resolve against the consolidated map.

- 2026-07-03 — Auth Phase 1 (NextAuth v5 + GitHub OAuth): installed `next-auth@5.0.0-beta.31` + `@auth/prisma-adapter@2.11.2` and set up the split-config pattern for edge compatibility. `src/auth.config.ts` holds the edge-safe slice (GitHub provider — auto-reads `AUTH_GITHUB_ID`/`_SECRET` — plus `jwt`/`session` callbacks that carry `user.id`, no adapter). `src/auth.ts` builds the full Node instance (`PrismaAdapter(prisma)` + `session.strategy: "jwt"`, spreads authConfig) exporting `auth`/`handlers`/`signIn`/`signOut`. `src/app/api/auth/[...nextauth]/route.ts` re-exports `handlers` as `GET`/`POST`. `src/proxy.ts` builds its own adapter-free `NextAuth(authConfig)` (keeps Prisma out of the edge runtime) and uses `export const proxy = auth((req) => …)` to 307-redirect unauthenticated `/dashboard*` visitors to `/api/auth/signin` with a `callbackUrl`; matcher excludes `api`/`_next`/`favicon`. `src/types/next-auth.d.ts` extends `Session.user` + JWT with `id`. DB models (User w/ password + Account/Session/VerificationToken) already existed from the initial migration, so no migration was needed. Documented `AUTH_SECRET`/`AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` in `.env.example` (real values already in local `.env`). No custom `pages.signIn` — uses NextAuth's default page. Verified with `pnpm lint` (clean), `pnpm build` (Proxy + auth route registered), and live curl: `/dashboard`→307 to sign-in w/ callbackUrl, `/`→200, `/api/auth/signin`→200, providers endpoint lists GitHub. Interactive GitHub OAuth round-trip left as a manual check (can't drive GitHub's consent screen headless).

- 2026-07-03 — Auth Phase 2 (Email/Password Credentials provider + registration): added a NextAuth Credentials provider via the split-config pattern. `src/auth.config.ts` gains an edge-safe placeholder — `Credentials({ credentials: { email, password }, authorize: () => null })` — so no bcrypt/Prisma is pulled into the edge/proxy runtime; `src/auth.ts` builds the real `credentialsProvider` (Node runtime) that Zod-validates the input, `findUnique`s the user by email, rejects OAuth-only users (no `password`), and `bcrypt.compare`s — returning `null` on any failure to keep errors generic — then swaps it into the config via `authConfig.providers.map(p => typeof p !== "function" && p.id === "credentials" ? credentialsProvider : p)` so GitHub passes through untouched. Added `src/lib/validations/auth.ts` (Zod `signInSchema` + `registerSchema` sharing email/`min(8)` password rules; `registerSchema.refine` enforces `password === confirmPassword`) and the `zod@4.4.3` dependency (per coding standards). Added `POST /api/auth/register` (`src/app/api/auth/register/route.ts`): parses JSON, validates with `registerSchema`, rejects duplicate emails (409), `bcrypt.hash(…, 12)` (matches the seed), creates the user, and returns `{ success, data, error }` with 201/400/409/500 status codes. `User.password` already existed from the initial migration, so no migration was needed. Verified with `pnpm lint` (clean), `pnpm build` (`/api/auth/register` registered), and live curl against the dev server: register 201 / duplicate 409 / password-mismatch 400 / short-password 400; providers endpoint lists **github + credentials**; full CSRF→callback sign-in with the correct password → 302 to `/dashboard` with a session carrying the JWT `id`, wrong password → 302 to `signin?error=CredentialsSignin`. GitHub OAuth unchanged (interactive consent still a manual check). Test left one user (`test+…@test.com`) on the Neon Development branch.

- 2026-07-03 — Auth Phase 3 (Custom Auth UI — Sign In / Register / Sign Out): built the custom sign-in/register pages and the sidebar user menu, replacing NextAuth's default page. Added `/sign-in` (`SignInForm.tsx` — `signIn("credentials", …)` with `redirect:false` + client-side `signInSchema` validation and error display, plus a "Sign in with GitHub" `signIn("github")` button), `/register` (`RegisterForm.tsx` — `registerSchema`-validated name/email/password/confirm fields, `POST /api/auth/register`, then auto sign-in; a `sonner` toast confirms success), and `/profile` (session-guarded placeholder, the avatar-click target). Shared `AuthCard.tsx` wraps both auth forms. Set `pages: { signIn: "/sign-in" }` in `auth.config.ts` and updated `proxy.ts` to redirect unauthenticated `/dashboard*` visitors there (was `/api/auth/signin`). Added a reusable `user-avatar.tsx` + `src/lib/avatar.ts` (`image` → use it, else initials from `name`, e.g. "Brad Traversy" → "BT"), consumed by the new `SidebarUser.tsx` — a real-session-user footer (avatar links to `/profile`) with a ShadCN `dropdown-menu` containing "Sign out" (`signOut()`), unblocking deferred audit item **M3** (sidebar no longer on mock user data). GitHub sign-in now links to an existing same-email account (`auth.config.ts` `signIn` callback / `allowDangerousEmailAccountLinking`). Installed ShadCN `dropdown-menu` + `sonner` (Toaster mounted in the root layout) and added `next.config.ts` `images.remotePatterns` for GitHub avatar hosts. Verified with `pnpm build` (all routes registered: `/sign-in`, `/register`, `/profile`; Proxy present). Merged to main via `/feature complete`.

- 2026-07-04 — Email Verification on Register (Resend): require email/password users to verify their address before signing in. Added the `EmailVerificationToken` Prisma model (email, unique token, 24h expiry) + migration `20260704002857_add_email_verification_token` (kept separate from NextAuth's own `VerificationToken`). Installed `resend@6.17.1`; `src/lib/email/resend.ts` holds the client + `EMAIL_FROM` (defaults to `DevStash <onboarding@resend.dev>`), `src/lib/email/verification-email.ts` builds the HTML/text email (HTML-escaped, no react-email dep). `src/lib/auth/verification.ts` centralizes the logic: `createVerificationToken` (one active token per email — deletes prior), `sendVerificationEmail`, `verifyEmailToken` (single-use; returns `{status, email}` for success/already-verified/expired/invalid and consumes tokens on success/expiry), `resendVerificationEmail` (anti-enumeration — silently no-ops for unknown/OAuth-only/already-verified), and `getBaseUrl` (prefers `AUTH_URL`/`NEXT_PUBLIC_APP_URL`, else request origin, else localhost). `POST /api/auth/register` now emails a link after creating the user (send failure non-fatal — returns `emailSent` flag); `RegisterForm.tsx` swaps the form for a "check your email" panel with a resend button instead of auto sign-in. Added `/verify-email` (`src/app/verify-email/page.tsx`, `force-dynamic`) rendering success/already-verified/expired(+resend)/invalid states, and `POST /api/auth/resend-verification` (always generic 200) with a reusable `ResendVerification.tsx` button (used on both the register panel and the sign-in unverified state). Sign-in gate: `auth.ts` `authorize()` throws `EmailNotVerifiedError extends CredentialsSignin` (`code = "EmailNotVerified"`) when the password is correct but `emailVerified` is null; `SignInForm.tsx` reads `result.code` (confirmed present in next-auth beta.31's `SignInResponse`, parsed from the callback URL) to show "Please verify your email" + a resend button. GitHub OAuth is unaffected (provider-verified). Seeded demo user already has `emailVerified` set, so its login still works. Documented `RESEND_API_KEY`/`EMAIL_FROM`/`AUTH_URL` in `.env.example`. Verified with `pnpm lint` (clean), `pnpm build` (all routes registered incl. `/verify-email`, `/api/auth/resend-verification`), and a live curl flow against the dev server: register 201 (unverified user + token row) → credentials sign-in blocked with `code=EmailNotVerified` → `/verify-email?token=…` sets `emailVerified` and consumes the token (0 left) → sign-in 302 to `/dashboard` with a session → invalid/missing token → "Invalid link" → resend for a now-verified user creates no token. Note: Resend's shared `onboarding@resend.dev` sender only delivers to the account owner's email in test mode (hence `emailSent:false` for `@example.com` test addresses). Test left one user (`verify-test-…@example.com`, verified) on the Neon Development branch.

- 2026-07-04 — DB maintenance script (`db:purge-users`): added `scripts/delete-users.ts` (run via `pnpm db:purge-users`, `tsx` like `db:test`) that deletes every user except the demo user (`demo@devstash.io`) and all of their content — items, collections, tags, custom item types, accounts, sessions — scoped by `userId != demo.id`, in FK-safe order inside a `$transaction`. System item types (owner-less) are kept. Safety rails: prints the target DB host + per-table counts, defaults to a **dry run** and only applies with `--yes`/`-y`, and aborts if the demo user is missing (so it can't wipe everything with no keep-target). Uses the same dotenv + Neon adapter setup as `scripts/test-db.ts`, targeting whatever `DATABASE_URL` points at. Verified with `pnpm lint` (clean) and run against the Neon **Development** branch: removed the 9 leftover auth-phase test users (`test+…`, `verify-test-…`) + 1 orphaned account, leaving exactly 1 user (demo) and the 7 system item types. (Earlier this session, the demo user was also recreated on the **production** branch via a one-off `INSERT` after the user deleted it — bcrypt-hashed `SEED_DEMO_PASSWORD`, `emailVerified` set.)

- 2026-07-04 — Toggle Email Verification (`EMAIL_VERIFICATION_ENABLED`): added a single env-var flag to turn the email-verification system on/off without code changes — motivated by Resend having no verified domain yet (the shared `onboarding@resend.dev` sender only delivers to the account owner, so verification locks out every other address). New `src/lib/auth/email-verification.ts` exports `isEmailVerificationEnabled()` (single source of truth): **enabled by default** (secure) — verification runs unless the var is set to `false`/`0`/`off`/`no`. Wired into every touchpoint: `POST /api/auth/register` now checks the flag — when off it skips `sendVerificationEmail`, marks the new user `emailVerified` on creation, and returns `verificationRequired: false` (plus `emailSent`); `auth.ts` `authorize()` only throws `EmailNotVerifiedError` when the flag is on; `resendVerificationEmail` (in `verification.ts`) early-returns when off so `POST /api/auth/resend-verification` stays a generic-200 no-op; `RegisterForm.tsx` reads `data.data.verificationRequired` — when false it auto signs-in (`signIn("credentials", …)` → `/dashboard` + success toast) instead of showing the "check your email" panel. `.env.example` documents the var. GitHub OAuth is provider-verified and unaffected either way; the seeded demo user already has `emailVerified` set. Verified with `pnpm lint` (clean) and `pnpm build` (all routes registered), plus a live curl matrix against the Neon **Development** branch: flag OFF → register `verificationRequired:false`/`emailSent:false` + unverified user signs into `/dashboard` (gate bypassed) + resend generic-200 no-op; flag ON → register `verificationRequired:true` + same user blocked with `code=EmailNotVerified`. ⚠️ Testing footnote: `next start` loads `.env.production` (→ Neon **production** branch `br-bold-snow-acimgndx`), so an early `pnpm start` test registration accidentally created one user on production; it was deleted with one-time approval (prod confirmed clean) and all further tests were pinned to Development via an inline `DATABASE_URL` override. Two test users were cleaned up afterward (the production one, and one on Development) — the user's own `jolitp@gmail.com` account on Development was deliberately left untouched (blanket `db:purge-users` was NOT used because it would delete it).

- 2026-07-04 — Forgot Password (reset via email): let email/password users reset a forgotten password, reusing NextAuth's `VerificationToken` table for reset tokens (no new model/migration — `identifier` holds the email, 1-hour TTL, single active token per email since `createResetToken` deletes prior ones). `src/lib/auth/password-reset.ts` centralizes the logic: `sendPasswordResetEmail` (anti-enumeration — silently no-ops for unknown emails and OAuth-only accounts with no `password`), `checkResetToken` (non-destructive status peek used by the reset page — `findFirst` since the model's PK is composite `[identifier, token]` with no standalone unique on `token`), and `resetPassword` (validates the token, `bcrypt.hash(…, 12)` like register/seed, consumes the token + siblings, and sets `emailVerified` if null since owning the link proves inbox control). `src/lib/email/reset-password-email.ts` builds the HTML/text email (mirrors `verification-email.ts`). New routes: `POST /api/auth/forgot-password` (always generic 200) and `POST /api/auth/reset-password` (`{token,password,confirmPassword}` → `{success,data,error}`, 400 on invalid/expired/mismatch). New pages: `/forgot-password` (`ForgotPasswordForm` → generic "check your email" panel) and `/reset-password` (`force-dynamic`; server-checks the token then renders `ResetPasswordForm` for valid, or expired/invalid states linking back to `/forgot-password`). Added a "Forgot password?" link to `SignInForm.tsx` and `requestResetSchema`/`resetPasswordSchema` to `validations/auth.ts`. Reused `getBaseUrl`, the Resend client, `AuthCard`, `buttonVariants`, and the `{success,data,error}` shape — no new env vars. Verified with `pnpm lint` (clean) and `pnpm build` (all 4 routes registered), plus a live curl flow against the Neon **Development** branch: forgot-password → generic 200 for existing **and** nonexistent emails (token created only for the real user), invalid email → 400; reset page → form for a valid token, "Invalid link" for bad/missing; reset POST → mismatch 400, valid success, **reused token 400** (single-use), token row consumed (0 left); sign-in with the **new** password → 302 to `/`, **old** password → `CredentialsSignin`. Test left one user (`reset-test-…@example.com`, verified) on the Development branch.

- 2026-07-04 — Profile Page (info, stats, change password, delete account): built out the `/profile` route (previously an auth-guarded placeholder from Auth Phase 3) into a full account page. Data layer `src/lib/db/profile.ts`: `getProfileAccount(userId)` (name/email/image/createdAt + a derived `hasPassword` boolean — never returns the hash) and `getProfileStats(userId)` (totals + per-type item breakdown that covers every system type and the user's own custom types via `where: { OR: [{ isSystem: true }, { userId }] }`, including zero-count types, using an `item.groupBy({ by: ["typeId"], _count: { _all } })` joined against the type list — scoped to `userId`, unlike the still-unscoped dashboard selectors). Change password: `changePasswordSchema` added to `validations/auth.ts` (currentPassword + `min(8)` newPassword + confirm, refined so confirm matches and new ≠ current); `src/lib/auth/change-password.ts` `changePassword()` verifies the current password with `bcrypt.compare`, rehashes at cost 12, returns a status union (`success`/`invalid-current`/`no-password`/`not-found`); `POST /api/auth/change-password` (session-guarded via `auth()`, `{success,data,error}`, 400/401/404) drives the client `ChangePasswordForm.tsx` (Zod-validated, toast on success, resets the form). Delete account: `src/lib/auth/delete-account.ts` `deleteAccount()` deletes in FK-safe order inside a `$transaction` (itemTag→item→tag→collection→custom itemType→account→session→user, all scoped to the one user — explicit rather than relying on User cascade because `Item→ItemType` is `Restrict`); `POST /api/auth/delete-account` (session-guarded) + client `DeleteAccountDialog.tsx` (new shadcn `alert-dialog`, controlled open state blocked while pending, then `signOut({ callbackUrl: "/" })`). The page is an async `force-dynamic` server component: `auth()` → `getProfileAccount` (redirects to `/sign-in?callbackUrl=/profile` if no session or the user row is gone) → `getProfileStats`, rendering header (reuses `UserAvatar` — GitHub image or initials — + `Intl.DateTimeFormat` join date), usage (`ProfileStats.tsx`: two total cards + an "Items by type" list using the shared `ITEM_TYPE_ICONS` map with per-type accent color), a change-password section shown **only when `hasPassword`** (hidden for GitHub-only accounts), and a danger-zone delete card. Installed shadcn `alert-dialog` (reused existing `@base-ui/react`, no new deps). Verified with `pnpm lint` (clean), `pnpm build` (all routes registered incl. `/api/auth/change-password`, `/api/auth/delete-account`), and a live curl flow against the Neon **Development** branch: unauth `/profile`→307 to `/sign-in`, both APIs→401; throwaway user register→sign-in→profile renders name/email/join date/stats/change-password/danger zone; change-password wrong current→400, correct→200, new password signs in & old rejected; delete-account→200, post-delete sign-in fails, **0 leftover rows** (cascade confirmed); demo user (read-only) shows 18 items / 5 collections and all 7 type counts with change-password visible (has password). Test user self-deleted — no residue on Development. Note: left the pre-existing unrelated `.env.example` working-tree edit ("inverted from course" note) uncommitted, per one-feature-per-commit.

- 2026-07-04 — Rate Limiting for Auth (Upstash Redis): throttle the auth endpoints with `@upstash/ratelimit` (sliding window) to blunt brute force / credential stuffing / email-endpoint abuse. New `src/lib/rate-limit.ts` builds a single `Redis` client from `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` (or `null` when unset) and five named `Ratelimit` instances — `login` (5/15m), `register` (3/1h), `forgotPassword` (3/1h), `resetPassword` (5/15m), `resendVerification` (3/15m) — each with its own `ratelimit:<name>` key prefix and a 1s `timeout`. `checkRateLimit(name, id)` returns `{ success, remaining, reset }` and **fails open** (returns success) when Upstash is unconfigured *or* the call throws/times out, so an outage never locks users out. `getClientIp(request)` reads the first `x-forwarded-for` entry (Vercel) then `x-real-ip` then `127.0.0.1`; `tooManyRequestsResponse(reset)` returns 429 with `{ success:false, error:"Too many attempts. Please try again in X minutes." }` + a `Retry-After` (seconds) header. Wired IP-keyed early-return 429s into the `register`/`forgot-password`/`reset-password` route handlers and an IP+email check into `resend-verification` (after the email is parsed). **Login** was the tricky case — NextAuth owns `/api/auth/callback/credentials`, so the limit is enforced inside `authorize(credentials, request)` in `src/auth.ts`: it reads the IP from the request, checks the `login` limiter by `${ip}:${email}` *before* the DB lookup, and throws `RateLimitedError extends CredentialsSignin` (`code = "RateLimited"`); rate-limit is imported only in `auth.ts` (Node), never `auth.config.ts`/`proxy.ts`, keeping Upstash out of the edge runtime. Frontend: `SignInForm.tsx` reads `result.code === "RateLimited"` → inline error + `sonner` toast; `RegisterForm`/`ForgotPasswordForm`/`ResetPasswordForm` toast on `res.status === 429` (ResendVerification already toasted `data.error`). Installed `@upstash/ratelimit@2.0.8` + `@upstash/redis@1.38.0`; documented the two env vars in `.env.example` (optional in dev = fail open). Verified with `pnpm lint` (clean), `pnpm build` (all routes registered), and a live run against the configured Development Upstash: resend-verification 3× 200 → 4th **429** (`Retry-After: 331`, "6 minutes"); login via the credentials callback → attempts 1–5 `code=credentials`, 6th+ **`code=RateLimited`**. IP-only routes share the identical helper/pattern (not hammered at runtime to avoid a 1-hour local lockout + register DB writes). Note: the `.env.example` commit also swept in the small pre-existing "inverted from course" `EMAIL_VERIFICATION_ENABLED` note that prior features had left uncommitted (it was entangled with the new Upstash block).

- 2026-07-04 — Items List View (`/items/[type]`): added a dynamic type-filtered items page. New `src/app/(app)/items/[type]/page.tsx` (`force-dynamic`) awaits `params`, `decodeURIComponent`s the `[type]` segment, and calls the new `getItemsByType()` in `src/lib/db/items.ts` — a case-insensitive `itemType.findFirst` (`name` `mode: "insensitive"`) then a newest-first `item.findMany({ where: { typeId } })` reusing the existing `ITEM_SELECT`/`toDashboardItem` shape + a shared `referenceNow`; returns `null` for unknown types so the page calls `notFound()` (404). The page renders a header (type icon/accent color via the shared `ITEM_TYPE_ICONS` map + `capitalize`d name + "N item(s)" count) and a responsive **two-column grid (`md:grid-cols-2`)** of the existing `ItemCard` (type-colored left border comes for free), plus a dashed empty-state for zero-count types. **Shell (user chose the route-group option):** moved `/dashboard` into a new `(app)` route group and hoisted its layout to `src/app/(app)/layout.tsx` (renamed `DashboardLayout`→`AppLayout`) so `/dashboard` and `/items/*` share the sidebar + top bar with no duplication — URLs unchanged (route groups don't affect paths; `@/`-aliased imports unaffected). Added `/items` to `PROTECTED_PREFIXES` in `src/proxy.ts` so item pages are auth-gated like the dashboard. The `[type]` segment matches the sidebar's `/items/${type.name}` links, which use the DB `ItemType.name` (`snippet`/`prompt`/`note`/`command`/`file`/`image`/**`link`** — note it's `link`, not `url`, per the seed). Verified with `pnpm lint` (clean), `pnpm build` (`/items/[type]` registered `ƒ Dynamic`, `/dashboard` intact, Proxy present), and a live curl flow (demo user) against the Neon **Development** branch: unauth `/items/snippet`→307 to `/sign-in` w/ callbackUrl; authed `/items/snippet`→200 (header "Snippet · 4 items", 4 cards w/ `#3b82f6` left borders, `md:grid-cols-2`); all 7 real types→200; zero-count types (note/image/file)→empty state; unknown types (`nonexistent`, `url`)→404; case-insensitive (`/items/Snippet`)→200.

- 2026-07-04 — Vitest setup (server actions + utilities): added **Vitest 4** (`pnpm test` / `pnpm test:watch`) for unit testing, deliberately scoped to **server actions and `src/lib` utilities — not React components** (Node environment, no jsdom / React Testing Library). `vitest.config.ts` uses `environment: "node"`, native `resolve.tsconfigPaths` for the `@/*` alias (Vitest 4 supports this without the `vite-tsconfig-paths` plugin, which was installed then removed), `globals` **off** (tests `import { describe, it, expect } from "vitest"` explicitly — avoids widening tsconfig `types`, which would disable the app-wide auto-included `@types/node`/React types), and `include: ["src/**/*.test.ts"]`. Starter suite (27 tests, colocated `*.test.ts`): `format.test.ts` (`formatRelativeTime`/`fileExtension`/`formatFileSize`), `avatar.test.ts` (`getInitials`/`avatarColor`), `validations/auth.test.ts` (all 5 Zod schemas incl. refine paths), `auth/email-verification.test.ts` (`isEmailVerificationEnabled` env matrix). Docs: folded testing into the workflow in `context/ai-interaction.md` (step 4 now runs `pnpm test` + `pnpm build`; new **Testing** section defining scope) and updated `CLAUDE.md` (replaced "No test framework is configured" with the `pnpm test` commands + scope note); added a `test` action to the `feature` skill (`SKILL.md`). DB-backed selectors (`src/lib/db/*`) and Prisma-touching auth logic deferred — they need Prisma mocking; there's no `src/actions/` dir yet (app uses API routes + server components), so utilities are the current test surface. Verified with `pnpm test` (27/27 pass), `pnpm lint` (clean), and `pnpm build` (all routes intact; test files type-check as part of the build).

- 2026-07-04 — Item Listing 3-Column Grid: made the `/items/[type]` listing grid responsive with a 3-column max — changed the grid in `src/app/(app)/items/[type]/page.tsx` from `grid gap-4 md:grid-cols-2` to `grid gap-4 md:grid-cols-2 lg:grid-cols-3` (1 column on mobile, 2 at `md` ≥768px, 3 at `lg` ≥1024px). CSS-only change (one Tailwind v4 class) — no data/logic changes, `ItemCard` untouched, dashboard grids left as-is. Verified with `pnpm lint` (clean), `pnpm test` (27/27 pass — unaffected), and `pnpm build` (`/items/[type]` still `ƒ Dynamic`).

- 2026-07-05 — Item Drawer: right-side slide-in drawer that is the item detail view (no separate item page), opening when an `ItemCard` is clicked on both the dashboard and `/items/[type]`. New `src/components/ui/sheet.tsx` — a shadcn-style Sheet built on the base-ui `Dialog` primitive (Root/Trigger/Close/Portal/Backdrop/Popup + Title/Description), side-variant slide animations (`data-open:slide-in-from-right` / `data-closed:slide-out-to-right` via tw-animate-css) and a baked-in close X. Data: added `getItemDetail(id)` + `ItemDetail` (extends `DashboardItem` with `collection {id,name}`, `fileUrl`, `createdAt`) to `src/lib/db/items.ts` via a shared `ITEM_DETAIL_SELECT`; **auth-gated but intentionally unscoped** (`findUnique({ where: { id } })`) to match the still-unscoped `getDashboardItems`/`getItemsByType` selectors — an owner-scoped first cut caused a 404 "Item not found" for every card when the signed-in user wasn't the items' owner (the demo user), since the lists show all items to everyone. New route `GET /api/items/[id]` (`src/app/api/items/[id]/route.ts`) — requires a session (`auth()`, 401), 404 on unknown id, `{success,data,error}`; not owner-scoped yet (re-add the `userId` filter here **and** in the list selectors together when per-user scoping lands, so they don't drift). Client state: `src/components/dashboard/item-drawer/item-drawer-context.tsx` (`ItemDrawerProvider` holds the clicked-card `DashboardItem` + open flag and mounts one shared `<ItemDrawer/>`; `useItemDrawer()` hook), wrapped around `<main>`'s children in `src/app/(app)/layout.tsx` so server-component pages can render cards that drive one drawer. `ItemDrawer.tsx` renders the header instantly from the card data, then fetches `/api/items/[id]` on open (AbortController-guarded, keyed by open+id) with a pulse skeleton while loading — sections: metadata badges (collection·language·"Updated …"), action bar (Copy — wired to clipboard; Favorite star yellow-when-active; Pin — both `aria-pressed`, reflecting state, mutations deferred), CONTENT (code `<pre>` / url link / file-info variants), TAGS, and a footer (Delete left, Edit right, disabled until detail loads). `ItemCard.tsx` became a `"use client"` component: the card is a `role="button"` (Enter/Space + focus-visible ring) calling `openItem(item)`. Relative time in the drawer uses a `fetchedAt` captured at fetch-resolution (not `Date.now()` in render) to satisfy React 19's purity rule; the result is stored keyed by item id so a stale response never shows on the wrong item. Scope = **display only** per spec (code editor / per-type extras + Favorite/Pin/Delete/Edit wiring come later). Verified with `pnpm lint` (clean), `pnpm build` (`/api/items/[id]` registered, all routes intact), `pnpm test` (27/27 — feature is DB-backed + components, outside the unit-test scope, so no new tests), a live curl matrix on the Neon **Development** branch (unauth→401; owner demo→200 with content/language/tags/collection; **non-owner throwaway user→200** after the unscoping fix, was 404; unknown id→404), and a Playwright walkthrough (sign in as demo → click pinned snippet → drawer matches `drawer-open.png`: badge/title/desc, React Patterns·typescript·Updated, Copy/Favorited-yellow/Pinned, code, tags, Delete/Edit; confirmed on `/items/link` a url item renders its link with no language badge and unpressed Favorite/Pin). Throwaway test user self-deleted via its own delete-account endpoint (0 residue; `db:purge-users` deliberately avoided — it would delete the user's own `jolitp@gmail.com` account).

- 2026-07-05 — Item Drawer — Edit Mode: clicking Edit in the item drawer now flips the same drawer into an inline edit form (no separate page), with Save/Cancel replacing the view. New `src/lib/validations/items.ts` — `updateItemSchema` (Zod, server-side source of truth): trims a required `title`, normalizes empty/whitespace optional text (`description`/`content`/`language`) to `null`, validates `url` (empty→null, else must be a valid URL), and trims/drops-empty/**de-duplicates** the `tags` array (dedupe avoids ItemTag PK clashes); `items.test.ts` covers all of it (+7 tests → 34 total). First `src/actions/` entry: `updateItem(itemId, input)` server action (`"use server"`, `{ success, data, error }`) — `auth()` session-guard, Zod `safeParse` (returns the first issue message), then an **ownership check** (`item.userId === session.user.id`) before writing; a mutation, so it is owner-scoped even though the read selectors stay unscoped. Data layer `updateItem(id, ownerId, data)` in `src/lib/db/items.ts` — a `$transaction` that `deleteMany`s the item's existing `ItemTag` joins then `item.update`s the scalars and re-creates the tag joins via per-owner `connectOrCreate` (`userId_name`), returning a fresh `getItemDetail` so the drawer refreshes without a second fetch (Tag rows are never deleted, only the joins). UI: `ItemEditForm.tsx` (controlled inputs, **no form library**) renders Title + Description always, and type-specific fields — Content (snippet/prompt/command/note), Language (snippet/command), URL (link) — sending the existing value for fields a type doesn't show so nothing is wiped; Save disabled when the title is empty, `sonner` toast on success/error, then `router.refresh()` so the card list reflects the change; Cancel returns to view mode discarding edits. Added `src/components/ui/textarea.tsx` (matches the Input styling). `ItemDrawer.tsx`: edit-mode is derived from an `editingId` state (equality with the open item) so switching cards or closing the drawer auto-exits edit mode **without a `setState`-in-effect** (which trips the `react-hooks/set-state-in-effect` lint rule); the header now prefers the fetched/refreshed `detail` over the clicked-card snapshot so a saved title/description shows up there too, not just in the body. Verified with `pnpm lint` (clean), `pnpm test` (34/34), `pnpm build` (`/api/items/[id]` + all routes intact), and a Playwright walkthrough as the demo user on the Neon **Development** branch: snippet edit → all fields shown, Save persisted (drawer body + header + card list all refreshed, tag added, "Updated just now"), then reverted to restore the seed item; link item → correct type-specific fields (URL, no content/language); invalid URL → server rejects with inline error + toast, no write; Cancel → discards, view mode unchanged. **Ownership decision (user chose to keep the check):** because the read path is intentionally unscoped (every signed-in user sees the demo user's seeded items), a non-owner — e.g. the user's own `jolitp@gmail.com` account — editing a demo item is (correctly) blocked with "You do not have access to this item" (screenshot: `context/screenshots/drawer-error-you-do-not-have-access-to-this-item.png`); to edit, sign in as `demo@devstash.io`. Residue: the persist/revert test left one orphaned `Tag` row (`verified`, demo user, no items) on the Development branch (update disconnects joins but never deletes Tag rows); left in place (destructive SQL needs explicit approval).

- 2026-07-05 — Delete Item: wired up the previously no-op Delete button in the item drawer footer. New `DeleteItemButton.tsx` (client) guards the action behind the existing shadcn `AlertDialog` (destructive media icon, item title in the copy, Cancel/Delete) — the dialog blocks backdrop/Escape close while the delete is in flight (`onOpenChange` gated on `pending`), shows a `Loader2` spinner, `sonner`-toasts success/failure, and on success calls back into `ItemDrawer.tsx` which `setOpen(false)`s the drawer and `router.refresh()`es so the deleted card disappears. Added the `deleteItem(itemId)` server action to `src/actions/items.ts` mirroring `updateItem` (`"use server"`, `{success,data,error}`): `auth()` session-guard → `item.findUnique` existence check ("Item not found") → ownership check (`userId === session.user.id`, else "You do not have access to this item") → db call, generic error on throw. Data layer `deleteItem(id, ownerId)` in `src/lib/db/items.ts` — a `$transaction` that `deleteMany`s the item's `ItemTag` joins then `deleteMany`s the item **scoped by `{ id, userId: ownerId }`** (defense-in-depth so a racey/stale call can't delete another user's item), in FK-safe order; Tag rows are left in place, matching `updateItem`. Added `src/actions/items.test.ts` (5 tests, +5 → 39 total) covering the action's guards with `auth`/Prisma/db-layer mocked: no-session, not-found, non-owner blocked, owner success (calls db with `(id, ownerId)`), and db-throw → generic error. **Ownership caveat (same as edit):** the read/list path stays intentionally unscoped, so a non-owner (e.g. `jolitp@gmail.com`) sees the demo user's seeded items but can't delete them — sign in as `demo@devstash.io` to delete seeded items. Verified with `pnpm test` (39/39), `pnpm lint` (clean), and `pnpm build` (all routes intact). Confirmation-dialog screenshot at `context/screenshots/delete-confirm-dialog.png`.
